enabled+passing cells: 168
backend    passing  verify  earned%  UNEARNED
dbi             86       8     9.3%        78
kvm              3       3   100.0%         0
ptrace          79      52    65.8%        27
TOTAL          168      63    37.5%       105

UNEARNED cells by mode: {'chaos': 1, 'custom': 1, 'replay': 1, 'strict': 102}

=== the 105 unearned cells (deterministic=1 from a SINGLE passing run) ===
  dbi      strict   backend-parity/anonymous_mmap_layout
  dbi      strict   backend-parity/anonymous_mmap_layout
  dbi      strict   backend-parity/anonymous_mmap_layout
  dbi      strict   backend-parity/argument_forwarding
  dbi      strict   backend-parity/argument_forwarding
  dbi      strict   backend-parity/argument_forwarding
  dbi      strict   backend-parity/cpuid_policy
  dbi      strict   backend-parity/cpuid_policy
  dbi      strict   backend-parity/cpuid_policy
  dbi      strict   backend-parity/executable_mmap
  dbi      strict   backend-parity/executable_mmap
  dbi      strict   backend-parity/executable_mmap
  dbi      strict   backend-parity/exit_status
  dbi      strict   backend-parity/exit_status
  dbi      strict   backend-parity/exit_status
  dbi      strict   backend-parity/exit_zero
  dbi      strict   backend-parity/exit_zero
  dbi      strict   backend-parity/exit_zero
  dbi      strict   backend-parity/file_mutation
  dbi      strict   backend-parity/file_mutation
  dbi      strict   backend-parity/file_mutation
  dbi      strict   backend-parity/file_read
  dbi      strict   backend-parity/file_read
  dbi      strict   backend-parity/file_read
  dbi      strict   backend-parity/heap_growth
  dbi      strict   backend-parity/heap_growth
  dbi      strict   backend-parity/heap_growth
  dbi      strict   backend-parity/hello_stdout
  dbi      strict   backend-parity/hello_stdout
  dbi      strict   backend-parity/hello_stdout
  dbi      strict   backend-parity/io_uring_fallback
  dbi      strict   backend-parity/io_uring_fallback
  dbi      strict   backend-parity/io_uring_fallback
  dbi      strict   backend-parity/listmount_unavailable
  dbi      strict   backend-parity/listmount_unavailable
  dbi      strict   backend-parity/listmount_unavailable
  dbi      strict   backend-parity/memory_advice
  dbi      strict   backend-parity/memory_advice
  dbi      strict   backend-parity/memory_advice
  dbi      strict   backend-parity/process_vm_readv_refusal
  dbi      strict   backend-parity/process_vm_readv_refusal
  dbi      strict   backend-parity/process_vm_readv_refusal
  dbi      strict   backend-parity/process_vm_writev_refusal
  dbi      strict   backend-parity/process_vm_writev_refusal
  dbi      strict   backend-parity/process_vm_writev_refusal
  dbi      strict   backend-parity/process_wait_accounting
  dbi      strict   backend-parity/process_wait_accounting
  dbi      strict   backend-parity/process_wait_accounting
  dbi      strict   backend-parity/process_wait_lifecycle
  dbi      strict   backend-parity/process_wait_lifecycle
  dbi      strict   backend-parity/process_wait_lifecycle
  dbi      strict   backend-parity/random_sources
  dbi      strict   backend-parity/random_sources
  dbi      strict   backend-parity/random_sources
  dbi      strict   backend-parity/scheduler_policy_queries
  dbi      strict   backend-parity/scheduler_policy_queries
  dbi      strict   backend-parity/scheduler_policy_queries
  dbi      strict   backend-parity/shared_anonymous_mmap
  dbi      strict   backend-parity/shared_anonymous_mmap
  dbi      strict   backend-parity/shared_anonymous_mmap
  dbi      strict   backend-parity/sigaction_state
  dbi      strict   backend-parity/sigaction_state
  dbi      strict   backend-parity/sigaction_state
  dbi      strict   backend-parity/sigaltstack_state
  dbi      strict   backend-parity/sigaltstack_state
  dbi      strict   backend-parity/sigaltstack_state
  dbi      strict   backend-parity/signal_disposition
  dbi      strict   backend-parity/signal_disposition
  dbi      strict   backend-parity/signal_disposition
  dbi      strict   backend-parity/sigprocmask_state
  dbi      strict   backend-parity/sigprocmask_state
  dbi      strict   backend-parity/sigprocmask_state
  dbi      strict   backend-parity/virtual_clock
  dbi      strict   backend-parity/virtual_clock
  dbi      strict   backend-parity/virtual_clock
  dbi      strict   backend-parity/virtual_pid
  dbi      strict   backend-parity/virtual_pid
  dbi      strict   backend-parity/virtual_pid
  ptrace   strict   backend-parity/anonymous_mmap_layout
  ptrace   strict   backend-parity/argument_forwarding
  ptrace   strict   backend-parity/cpuid_policy
  ptrace   strict   backend-parity/executable_mmap
  ptrace   strict   backend-parity/exit_status
  ptrace   strict   backend-parity/exit_zero
  ptrace   strict   backend-parity/file_metadata
  ptrace   strict   backend-parity/file_mutation
  ptrace   strict   backend-parity/file_read
  ptrace   strict   backend-parity/heap_growth
  ptrace   strict   backend-parity/hello_stdout
  ptrace   strict   backend-parity/io_uring_fallback
  ptrace   strict   backend-parity/listmount_unavailable
  ptrace   strict   backend-parity/memory_advice
  ptrace   strict   backend-parity/process_vm_readv_refusal
  ptrace   strict   backend-parity/process_vm_writev_refusal
  ptrace   strict   backend-parity/process_wait_accounting
  ptrace   strict   backend-parity/process_wait_lifecycle
  ptrace   strict   backend-parity/pthread_lifecycle
  ptrace   strict   backend-parity/random_sources
  ptrace   strict   backend-parity/scheduler_policy_queries
  ptrace   strict   backend-parity/shared_anonymous_mmap
  ptrace   strict   backend-parity/virtual_clock
  ptrace   strict   backend-parity/virtual_pid
  ptrace   chaos    determinism-stress/order-violation
  ptrace   custom   system-utils/clock-determinism
  ptrace   replay   system-utils/record-getpid
