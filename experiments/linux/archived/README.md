# Archived / superseded Linux-QEMU material

Historical documents kept for provenance. They are superseded by the parent
`experiments/linux/README.md` and the maintained in-repo docs
(`hermit/docs/QEMU_BOOT.md`, `hermit/tests/qemu-boot/`).

- `qemu-linux-compat-harness_20260723.md` — the original 2026-07-23 self-contained
  compat-boot harness README (copied from `experiments/qemu-linux/README.md`).
  Superseded by `../demo.sh` + `../SAMPLE_OUTPUT.txt` and the 07-27 consolidation.
  The live harness still exists at `experiments/qemu-linux/` and is intentionally
  left in place (referenced by other work); only its README is archived here.
