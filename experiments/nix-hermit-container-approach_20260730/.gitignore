# Binaries — regenerated by recipe/nix-under-hermit.sh (never commit binaries).
hostlibs/
recipe/pidfd_probe
# Large/verbose captured logs — keep only curated evidence.
logs/strace*.log
logs/probe.log
logs/*.exit
