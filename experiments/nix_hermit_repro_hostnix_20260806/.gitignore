# Raw per-build nix/hermit logs: high-volume, machine-specific, regenerated by
# run.sh. Curated summaries (the CSV rows each run emitted, the spawn-cost
# table, the cmake-hang reproducer output) are tracked under summaries/.
logs/
