[package]
name = "shmem-pod-preload-guest"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

# Deliberately empty: this program has no pod or preload-shim dependency.
[dependencies]
