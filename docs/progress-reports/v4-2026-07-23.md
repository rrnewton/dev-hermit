# Hermit Progress Report v4 — 2026-07-23

Follows the `progress-rubric` skill. Numbers are live measurements; where a
value is carried from the automated v3 run it is labelled. Supersedes the
narrative content of the misplaced `ai_docs/transient/20260722_progress_rubric_v2.md`.

## Test context

| Field | Value |
| --- | --- |
| Repo | rrnewton/hermit (submodule) |
| Commit tested | `a85f015b5607c2a08ec5b8fb19dcebf00ac18d91` (`a85f015`) |
| Parent branch | devbig-lead |
| Date (UTC) | 2026-07-23 |
| Backend | ptrace (`--strict` / `--strict --verify`) |
| Host CPU | AMD EPYC 9D85 158-Core Processor |
| Kernel | 6.17.13-0_fbk0_crackerjackhost_0_g2b4321c50d79 |
| perf_event_paranoid | 1 |
| Toolchain | rustc 1.99.0-nightly (be8e82435 2026-07-11) |
| Guest runtimes present | gcc/g++, python3, python3.9, node, redis-server, sqlite3, java, perl, go |

## Headline status

- **Overnight PR velocity:** ~20 numbered PRs landed on `hermit` main since
  2026-07-22 evening (see "Recently landed PRs"). ≥10 are substantive
  determinism/backend fixes; the rest are documentation of the verification
  matrix.
- **Task graph:** 1075 / 1115 tasks complete (**96%**) at time of writing
  (`tg` top-line).
- **Record/Replay pipe fix LANDED:** the record-mode pipe deadlock is fixed —
  `#227` (harvest ready external IO past pollers) + `#230` (classify internal
  pipes as `InternalIOPolling`, not `BlockingExternalIO`) + `#226`
  (`--verify` insensitive to `InternalIOPolling` retry count) + `#235`
  (record internal-pipe read data on the `InternalIOPolling` path).
- **QEMU boot:** boots under Hermit **with relaxations** (headline goal
  reachable, not yet clean `--strict`). Tracked as a relaxed-mode success, not a
  full-strict determinism witness.

## Language / runtime compatibility (live, this host)

Backend ptrace; `hermit run --strict --verify` unless noted. "L2" = strict+verify.

| Language / runtime | Status under Hermit | Evidence |
| --- | --- | --- |
| **C** | PASS L2 | gcc -O2 concurrency + IPC + epoll/poll/select/timerfd/signalfd batches all byte-identical run1/run2 (batches 27/28/30/35) |
| **C++** | PASS L2 | gcc/g++ built binaries determinize; single-proc & thread-sync clean |
| **Rust** | PASS (build/run) | Hermit itself + coreutils-class binaries; vfork-heavy build steps still flag on fork scheduling |
| **Python** (3.9 / CPython) | PASS L2 | math, sum, seeded random, itertools, sqlite3 in-mem, json, csv, xml, re — all deterministic (batches 24/25/26) |
| **Perl** | PASS L2 | regex `=~ /(\w+)/g` join deterministic (batch 26) |
| **Go** | PASS (runtime present) | `/usr/bin/go` available; single-proc programs determinize |
| **Java** (OpenJDK) | PASS L2 | hello determinizes at L2 (~10s) after LogicalTime saturation fix `#219`/`#223`; needs `--tmp=/tmp` for host-compiled classes |
| **Node.js** | PASS (promises) / flaky (timers) | promise/microtask async 13/13 deterministic; `setTimeout` flakes under `--verify` (libuv reads CLOCK_MONOTONIC into control flow) |

Real-app L2 matrix (this session, batch "real-apps"): **bc, awk, sort, sed,
sqlite3** — 5/5 PASS `--strict --verify`, outputs correct.

## Summary table (automated suites — carried from v3 run @ `2599e1e`)

| Suite | Command | Result |
| --- | --- | --- |
| Working-envelope L1-L4+rr | `validate.sh --envelope-only` | l1..l4 = 3/3, rr = 3/3 (probes: true, echo, date; l4_reps=20) |
| Record/replay | `cargo test -p hermit --test record_replay` | 24 passed, 0 failed, 0 ignored |
| App: sqlite_veryquick | `cargo test -p hermit --test sqlite_veryquick` | 1 passed, 0 failed, 1 ignored |
| App: redis_strict | `cargo test -p hermit --test redis_strict` | 3 passed, 0 failed, 1 ignored |
| App: python_stdlib | `cargo test -p hermit --test python_stdlib` | 1 passed, 0 failed, 1 ignored |

> The strict/fail-closed ratchet aborted at inventory in the v3 run
> (`curl_version_is_deterministic_under_strict_verify` ignored but not
> allowlisted). Re-run needed at `a85f015` to refresh these counts.

## Recently landed PRs (hermit main, since 2026-07-22 evening)

Determinism / backend / fixes:
- `#235` detcore: record internal-pipe read data on the InternalIOPolling path
- `#233` Fix KVM backend dispatch + private-flag futex timeout classification
- `#234` Route DBI backend through `reverie_dbi::DbiRunner` (DBI M2b)
- `#229` hermit-cli: prove Detcore drives KvmGuest via `run_with_tool` (KVM M3)
- `#230` classify internal pipes as InternalIOPolling (R/R pipe fix)
- `#227` Fix record-mode pipe deadlock: harvest ready external IO past pollers
- `#226` Make `--verify` insensitive to InternalIOPolling retry count
- `#225` Add reverie-dbi dependency to hermit-cli (DBI M2a)
- `#221` Fix vfork priority
- `#219` detcore-model: saturate LogicalTime addition (Java overflow fix)
- `#223` Java overflow fix v2
- `#218` Wire hermit-cli to reverie-kvm backend
- `#214` Fix scheduler panic on SIGALRM re-arm after an alarm fires
- `#215`/`#213` Ungate `--backend dbi` (dispatch to DynamoRIO `run_dbi`)
- `#212` Bump reverie pin to working DBI rev
- `#211` validate: auto-apply locally-validated PR label on a green run

Docs: `#217`, `#224`, `#220`, plus WHATS_WORKING batches 18-41.

## Fleet / CI status

- **hermit main:** GitHub-hosted `Docs` gate GREEN on all completed runs; the
  self-hosted `Rust` workflow is backlogged/queued and is env-level red (not a
  required merge gate; precedent: main unprotected, gate = GitHub-hosted green).
- **reverie main:** GitHub-hosted `Rust` workflow GREEN on all recent pushes
  (park/unpark executor, KVM StraceTool `#39`, DBI Guest stack+tail_inject,
  backend contract `#23`, CI warnings `#38`).

## Known blockers / follow-ups

- Fork-heavy `--verify` (vfork scheduling race): gcc/rustc/bash still flag at
  `vfork()`; cmake configure times out. Fix = deterministic fork/vfork
  scheduling. `#221` addresses vfork priority; full path still open.
- git & fbpython `--verify` diverge at `gettimeofday` (virtual-time doubling
  under multithreading), not a syscall gap.
- FIFO named-pipe open-rendezvous still deadlocks under `--strict` (anonymous
  pipes/sockets/eventfd are clean).
- Re-run `scripts/test-fail-closed.sh` and `validate.sh --envelope-only` at
  `a85f015` to refresh the automated suite counts (this report carries the v3
  numbers).
