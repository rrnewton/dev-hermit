# Hermit Morning Coordination Report v5 - 2026-07-27

This is a coordination snapshot of landed work, pull-request health, backend
ratchets, and CI. It is not a fresh cross-mode application benchmark under the
`progress-rubric`; backend totals below come from the matrix committed on the
exact Hermit `main` SHA and are labelled accordingly.

## Snapshot

| Field | Value |
| --- | --- |
| Captured | 2026-07-27T08:48:26Z |
| Hermit `main` | `10e43392db25289bd4cee9069e02d02392a605cc` (#885) |
| Reverie `main` | `043aa00d9ea2dc27fa0e9e887c14f47de486bc68` |
| Host kernel | `6.17.13-0_fbk0_crackerjackhost_0_g2b4321c50d79` |
| Coordinator open PRs | 39 total: 21 CI-green, 18 CI-red |
| Review labels | 4 `post-facto-review`, 35 without that label |

Both primary checkouts were clean, on `main`, and matched `origin/main` when
this snapshot was taken.

## Executive Summary

- Fifteen recent Hermit merges advanced compatibility and backend coverage:
  eight LiteInst floor increments, six product changes, and one CI scheduling
  change. The parent pin advances from `6185d4a` to `10e43392`.
- The landed backend-parity matrix now records ptrace 13/13, DBI 12/13, and KVM
  11/13. DBI's remaining row is `pthread_lifecycle`; KVM's remaining rows are
  `process_wait_lifecycle` and `random_sources`.
- Current Hermit `main` is not terminal green. At the exact head SHA, the merge
  gate succeeded while hosted and self-hosted workflows remain in progress.
- CI color overstates readiness: all 21 CI-green open PRs have unresolved
  substantive adversarial-review findings. None should land solely because
  `pr_status.py` reports green.
- The LiteInst configured label floor reached 430, but review found only 429
  distinct exact commands because two labels execute `/usr/bin/patch
  --version`. Treat 430 as the declared label floor, not a clean distinct-tool
  coverage figure.

## Pull Request Queue

`./ci-hub/bin/pr-status` reported:

| State | Count | Interpretation |
| --- | ---: | --- |
| Open | 39 | Hermit and Reverie PRs shown by the coordinator status script |
| CI-green | 21 | Checks green, but each has an unresolved review blocker |
| CI-red | 18 | Failed, cancelled, missing, or otherwise non-green checks |
| `post-facto-review` | 4 | Hermit #794, #775; Reverie #163, #156 |

The green review blockers cluster as follows:

- Backend or record/replay bypasses: #882, #881, #876, #872, #869, #859,
  #857, #855, #852, #848, #847, #841, and #839.
- Alternate-path sanitization gaps: #877 and #861.
- ABI or lifecycle semantics not proven: #880, #868, #862, #860, and #853.
- KVM integration dependency: #874 depends on unlanded Reverie #172 and still
  lacks a successful snapshot-resume witness and a hosted QEMU/KVM gate.

The red queue includes active new work (#889, #888, #887, and #886), broader
kernel and procfs work (#883, #873, #865, #863, #856, #851, #846, #843), the
SaBRe lane (#845), two post-facto Hermit PRs (#794 and #775), and Reverie #172,
#163, and #156. Red does not by itself establish that a PR is obsolete.

## Recently Merged

The latest 15 merged Hermit PRs, newest first:

| PR | Title | Merge SHA |
| ---: | --- | --- |
| #885 | Ratchet memory advice compatibility | `10e43392` |
| #884 | Expand LiteInst compatibility floor to 430 | `c0cde458` |
| #879 | Expand LiteInst compatibility floor to 405 | `85e63cc7` |
| #878 | Ratchet executable mmap compatibility | `2d7de5ee` |
| #875 | Expand LiteInst compatibility floor to 380 | `dc1f742d` |
| #871 | Ratchet DBI process wait lifecycle | `a03f94e8` |
| #870 | Expand LiteInst compatibility floor to 355 | `cd201b00` |
| #867 | Expand LiteInst compatibility floor to 330 | `d4f27638` |
| #866 | Normalize host-global socket statistics | `6185d4a6` |
| #864 | Expand LiteInst compatibility floor to 305 | `5e84a1ef` |
| #854 | Move slow hosted compatibility diagnostics to weekly validation | `7942acf8` |
| #850 | Expand LiteInst compatibility floor to 280 | `8a5b5515` |
| #849 | Determinize root random sources under DBI | `42ef2b8b` |
| #844 | Determinize process isolation syscalls | `b8a70faf` |
| #842 | Expand LiteInst compatibility floor to 255 | `ca9c51cf` |

## Backend Ratchets

These are landed matrix declarations at `10e43392`, not fresh executions for
this report.

| Backend | Matrix | Remaining declared gap | Recent landed progress |
| --- | ---: | --- | --- |
| ptrace | 13/13 (100%) | none in this matrix | memory advice and executable mmap across all backends |
| DBI | 12/13 (92%) | `pthread_lifecycle` | memory advice, root random sources, process waits, executable mmap |
| KVM | 11/13 (85%) | `process_wait_lifecycle`, `random_sources` | memory advice and executable mmap |
| LiteInst | 430 labels configured | 429 distinct exact commands; duplicate patch probe | floor raised from 255 to 430 in eight merges |
| SaBRe | no new landed ratchet in this window | #845 remains red | unchanged |

Hermit #874 is credible QEMU/TCG Linux-boot prototype work inside the KVM
process personality, but it is unlanded and therefore does not change the KVM
total. It is not evidence of direct Linux-on-Hermit-KVM support or successful
snapshot-resume.

## Main CI Health

At `10e43392db25289bd4cee9069e02d02392a605cc`:

| Workflow | Run | State |
| --- | ---: | --- |
| Merge Gate | 30251087002 | success |
| Hosted tests | 30250984683 | in progress |
| Host-dependent tests | 30250984638 | in progress |

The latest three-run view therefore does not establish terminal main health.
Reverie's latest `Rust` workflow succeeded at
`043aa00d9ea2dc27fa0e9e887c14f47de486bc68` (run 30238716123).

## Morning Priorities

1. Obtain terminal hosted and self-hosted results at `10e43392`.
2. Resolve review findings before landing any of the 21 CI-green PRs. The most
   common defect is an implementation that covers ptrace while DBI copied-child
   or record/replay paths still reach the host.
3. Repair the LiteInst floor's duplicate-command accounting and bind future
   floor evidence to the exact tested SHA before raising it again.
4. Land Reverie #172 only after its own review and CI requirements, then rerun
   Hermit #874 with a successful snapshot-resume witness and hosted gate.
5. Triage the 18 red PRs by value and ownership; do not infer obsolescence from
   CI color alone.

## Evidence Commands

```text
./ci-hub/bin/pr-status
with-proxy gh pr list -R rrnewton/hermit --state merged --json number,title -L 15
with-proxy gh run list -R rrnewton/hermit --branch main -L 3
with-proxy gh run list -R rrnewton/reverie --branch main --workflow Rust -L 1
git -C hermit status --short --branch
git -C reverie status --short --branch
```
